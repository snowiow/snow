set sendmail  = "/usr/bin/msmtp -a privat"
set from = "marcel.patzwahl@posteo.de"
set folder = ~/Mail
set spoolfile = ~/Mail/Privat/INBOX
set postponed = ~/Mail/Privat/Drafts
set record = ~/Mail/Privat/Sent
set trash = ~/Mail/Privat/Trash

unmailboxes *
source mailboxes_Privat

# vim: syntax=muttrc
